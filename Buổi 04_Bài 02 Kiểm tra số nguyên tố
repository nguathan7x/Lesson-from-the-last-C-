#include<bits/stdc++.h>
using namespace std;
bool KTSNT(long long n)
{
	for(long long i=2;i<=sqrt(n);++i)
	{
		if(n%i==0)
			return false;
	}
	return true;
 } 
 int main()
 {
 	long long n;
 	cin>>n;
 		if(n>=0){ 
	if(KTSNT(n)&&n!=1)
		cout<<"YES";
		else 
	cout<<"NO";
 }return 0;
}
